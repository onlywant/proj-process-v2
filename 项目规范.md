# 项目台账与全国全景系统项目规范

## 1. 项目定位

本系统是一个面向电脑、Android 和 iOS 浏览器的响应式 Web 应用，用于长期维护项目台账、跟踪各省份项目进展、查看全国项目分布和追溯历史更新。

系统的核心不是任务分派或待办管理，而是维护一张可持续更新、可快速检索、可按省份分析的项目主表。

系统应满足：

- 以项目台账为唯一业务数据源
- 以“省份 + 项目”为核心维护维度
- 能快速找到用户需要的项目信息
- 能直接看到项目当前阶段、健康状态和最新进展
- 每次更新都保留历史，不能覆盖历史事实
- 项目长期没有更新时能够主动提示
- 电脑端和移动端均易于使用
- 界面简洁、美观、克制，减少重复和无用信息

## 2. 产品范围

### 2.1 一级导航

系统只保留以下业务导航：

1. 项目台账
2. 全国全景
3. 系统配置，仅管理员可见

“系统配置”用于用户、角色、字典和其他系统级设置。普通用户不能看到该入口，也不能通过前端导航进入系统配置页面。

用户个人信息不作为桌面端左侧一级导航展示。不同终端的用户入口如下：

- 桌面端：页面右上角显示当前用户和角色，点击后打开个人菜单。
- 手机端：导航名称为“我的”，用于查看个人信息、修改密码和退出登录。
- 管理员在“系统配置”中管理其他用户；普通用户只能管理自己的个人信息。

### 2.2 项目台账包含的功能

所有台账相关功能均归入“项目台账”，包括：

- 项目列表
- 项目新增和编辑
- 项目详情
- 更新项目进展
- 查看项目历史
- 项目搜索、筛选和排序
- Excel 导入
- Excel 导出
- 更新周期提示

### 2.3 明确不建设的功能

当前版本不建设或不单独建设：

- 待办中心
- 工作台
- 人员视图
- 独立的更新记录导航
- 独立的提醒中心
- 地市层级管理
- 项目删除
- 批量编辑
- 项目附件上传、下载和管理

更新记录只在具体项目详情中查看；更新提醒只在项目台账中体现。

## 3. 核心业务对象

系统的核心业务对象是“省份项目记录”。

同名项目在不同省份的进展可能不同，因此必须作为不同记录维护。例如，江苏的某项目和浙江的同名项目不能共用当前状态或进展。

### 3.1 项目唯一性

项目记录默认使用以下组合识别：

```text
省份 + 项目名称 + 项目年份
```

如果业务上存在同一省份、同一年份、同名项目，还应增加项目编号作为辅助唯一标识。

### 3.2 省份范围

当前系统只按省份维护，不增加地市层级。

“经信局”、政府主管部门、省公司部门等机构不作为地市对象，而作为项目相关单位或支撑部门记录在项目字段中。

## 4. 项目字段

### 4.1 基础信息

- 项目编号，可选
- 省份，必填
- 项目名称，必填
- 项目类型
- 项目具体类型
- 项目年份
- 主责部门
- 拓展组项目负责人
- 项目牵头参与单位

### 4.2 金额和业务信息

- 项目总金额（万元）
- 智芯合同金额（万元）
- 申报成功时间
- 智芯角色
- 智芯内部支撑部门
- 省公司支撑部门
- 政府主管单位或其他相关单位

现有 Excel 台账中的字段应尽量完整保留。字段可以根据业务变化扩展，但新增字段不能破坏现有导入数据。

### 4.3 当前状态信息

- 项目阶段状态
- 项目健康状态
- 当前进展
- 问题标签
- 问题说明
- 下一步计划
- 最近更新时间
- 最近更新人

## 5. 项目状态

项目阶段和项目健康状态必须分开维护。

### 5.1 项目阶段状态

项目阶段用于描述项目处于哪个业务阶段，默认选项包括：

- 策划中
- 申报中
- 已立项
- 执行中
- 已完成
- 暂停
- 失败
- 终止

阶段状态可以由管理员在“系统配置”中维护和扩展。

### 5.2 项目健康状态

健康状态用于快速判断项目是否需要关注，默认选项包括：

- 良好
- 正常
- 有问题
- 暂无信息

健康状态建议使用以下视觉含义：

- 良好：绿色
- 正常：蓝色
- 有问题：红色
- 暂无信息：灰色

当健康状态为“有问题”时，必须填写问题说明；建议同时选择问题标签，例如：

- 进度滞后
- 材料缺失
- 需求未确认
- 协作方未响应
- 审批卡点
- 合同问题
- 预算问题
- 其他

一个项目可以同时处于“申报中”和“有问题”等状态，两个状态不得互相覆盖。

## 6. 项目进展更新

### 6.1 更新要求

每个项目必须设置更新周期，至少在规定周期内更新一次进展。

即使本周期没有新的实际进展，也必须提交更新，例如：

```text
本周期暂无新进展，项目按原计划推进。
```

### 6.2 更新周期

默认支持：

- 每周
- 每两周
- 每月
- 自定义天数

每个项目至少保存：

- 更新周期
- 最近更新时间
- 下次应更新时间
- 更新状态

系统根据最近更新时间和下次应更新时间自动判断：

- 正常：距离下次更新时间较充足
- 临近更新：距离下次更新时间不足 7 天
- 逾期未更新：超过下次应更新时间

具体天数可以配置，但不能关闭项目更新周期检查。

### 6.3 更新表单

用户更新项目时至少填写：

- 项目阶段状态
- 项目健康状态
- 本次进展
- 问题说明，健康状态为“有问题”时必填
- 下一步计划
- 备注，可选

保存更新后，系统必须同时：

1. 更新项目当前快照；
2. 生成一条历史记录；
3. 记录更新人和更新时间；
4. 重新计算下次更新时间；
5. 更新项目台账中的更新提醒状态。

## 7. 历史记录和审计

### 7.1 进展历史

每次更新项目进展都必须生成一条不可被普通编辑覆盖的历史记录，至少包含：

- 更新时间
- 更新人
- 项目阶段状态
- 项目健康状态
- 问题标签
- 进展内容
- 问题说明
- 下一步计划
- 备注

历史记录按时间倒序展示，项目详情中使用时间线呈现。

### 7.2 历史修正

历史记录原则上只追加，不直接删除或覆盖。

如发现历史内容错误，应新增修正记录，并保留原始记录和修正人信息。

### 7.3 字段变更审计

项目基础信息发生重要变化时，应记录：

- 操作人
- 操作时间
- 修改字段
- 修改前内容
- 修改后内容

## 8. 项目台账

### 8.1 页面定位

项目台账是系统默认首页和主要工作区域。用户登录后应直接进入项目台账，而不是进入统计工作台。

### 8.2 表格默认字段

主表优先展示高频使用字段：

- 省份
- 项目名称
- 项目年份
- 项目类型
- 项目阶段
- 健康状态
- 最新进展
- 项目负责人
- 更新状态
- 最近更新时间

其他字段通过详情抽屉或列配置查看，避免主表过宽、信息重复或影响阅读。

### 8.3 搜索和筛选

支持：

- 项目名称搜索
- 省份筛选
- 项目年份筛选
- 项目类型筛选
- 项目阶段筛选
- 健康状态筛选
- 负责人筛选
- 更新状态筛选
- 项目进展和问题说明关键词搜索
- 多条件组合筛选
- 按更新时间、项目名称、状态等排序

提供常用快捷筛选：

- 全部项目
- 有问题项目
- 正常项目
- 良好项目
- 临近更新
- 逾期未更新
- 最近更新

### 8.4 表格操作

支持：

- 新增项目
- 编辑项目
- 更新进展
- 查看详情
- 查看历史
- 导入 Excel
- 导出当前筛选结果
- 导出完整台账

省份和项目名称等关键字段默认固定，方便用户横向浏览时保持上下文。

## 9. 全国全景

### 9.1 页面定位

全国全景用于从全国范围观察项目分布、发现重点省份，并快速进入某个省份的项目列表。

全国全景不是第二套项目数据，所有统计和项目列表必须实时来源于项目台账。

### 9.2 视觉形式

全国全景采用科技风格的可交互三维风格模拟地图，不要求接入真实地理地图数据：

- 深色背景
- 立体或浮雕式中国地图轮廓
- 省份边界发光
- 项目健康状态映射颜色
- 项目数量使用数字、标记或光点辅助展示
- 支持适度的视角移动和高亮动画

动画不能影响项目阅读和操作，不得使用持续快速旋转、过度闪烁或遮挡信息的效果。

### 9.3 省份交互

点击省份后：

1. 该省份高亮或放大；
2. 其他省份降低亮度；
3. 右侧显示该省份汇总信息；
4. 右侧列出该省份的项目；
5. 点击项目可打开项目详情或跳转项目台账。

省份汇总至少包括：

- 项目总数
- 良好项目数
- 正常项目数
- 有问题项目数
- 暂无信息项目数
- 临近更新项目数
- 逾期未更新项目数
- 最近更新时间

### 9.4 地图筛选

地图支持按以下条件重新统计和着色：

- 健康状态
- 项目阶段
- 项目类型
- 项目年份
- 更新状态

地图中的机构信息不单独形成地图层级。经信局、政府部门、省公司部门等信息随项目展示。

## 10. 导入和导出

### 10.1 Excel 导入

支持使用现有项目台账 Excel 模板导入数据。

导入流程必须包括：

1. 上传文件；
2. 识别表头；
3. 校验必填字段和字段格式；
4. 根据“省份 + 项目名称 + 项目年份”识别新增和更新；
5. 展示导入预览；
6. 用户确认后正式导入；
7. 保存导入批次和操作人。

预览结果至少显示：

- 新增记录数
- 更新记录数
- 重复记录数
- 校验失败记录数
- 错误原因

导入不能直接覆盖数据库中的历史进展记录。Excel 中的当前进展如被识别为更新，应形成新的历史记录，并标记来源为 Excel 导入。

### 10.2 Excel 导出

支持：

- 导出当前筛选结果
- 导出完整项目台账
- 导出项目历史记录

导出内容应包含必要的字段名称、当前状态、更新周期和最近更新时间。

## 11. 用户、角色和系统配置

### 11.1 登录

系统必须提供登录功能。

默认管理员账户：

- 用户名：`admin`
- 密码：`admin123`

首次部署后应支持管理员修改默认密码。

### 11.2 角色

系统只支持以下两类角色：

- 管理员：管理用户、字典和全部项目数据
- 普通用户：查看项目并新增、编辑、更新项目，执行 Excel 导入和导出

管理员同时拥有普通用户的全部业务权限。当前版本不设置只读角色，所有启用用户均可查看和编辑项目数据。

### 11.3 导航可见范围

所有角色都可以看到：

- 项目台账
- 全国全景

只有管理员可以看到：

- 系统配置

系统配置入口不得对普通用户显示。即使普通用户直接访问系统配置地址，后端也必须进行权限校验并拒绝无权限请求。

### 11.4 用户功能

管理员在“系统配置”中支持：

- 用户列表
- 新增用户
- 启用或停用用户
- 修改用户角色

所有角色在桌面端右上角的个人菜单或手机端“我的”中支持：

- 修改密码
- 查看当前用户信息
- 退出登录

## 12. 响应式 Web 设计

### 12.1 桌面端

桌面端左侧导航显示：

- 项目台账
- 全国全景
- 系统配置，仅管理员可见

当前登录用户和角色显示在页面右上角。点击右上角用户区域后，显示个人信息、修改密码和退出登录等操作。

桌面端重点支持：

- 大表浏览
- 多条件筛选
- 列配置
- Excel 导入导出
- 地图与项目列表联动

### 12.2 手机端

手机端采用适合小屏幕的导航方式，个人入口名称统一为“我的”。“我的”中包括：

- 当前用户信息
- 当前角色
- 修改密码
- 退出登录

管理员可以在“我的”中进入“系统配置”；普通用户不显示该入口。

手机端不强行展示完整宽表，而是自动切换为项目卡片或分组列表。

移动端重点支持：

- 搜索项目
- 按省份和状态筛选
- 查看项目详情
- 更新项目进展
- 查看历史记录
- 查看全国全景

更新按钮、保存按钮和返回按钮应固定在容易触达的位置，避免用户在长页面中反复滑动寻找操作入口。

### 12.3 浏览器兼容

优先适配：

- Chrome
- Edge
- Safari
- Android Chrome
- iOS Safari

布局、字体、表格、弹窗、地图和表单在不同浏览器下必须进行基本验证。不能依赖单一浏览器特有的行为实现核心功能。

### 12.4 技术与部署约束

- 后端使用 Go 语言实现。
- 数据库使用 SQLite。
- 前端使用 Vue 3、TypeScript 和 Vite，构建响应式跨平台 Web 应用。
- 前端必须支持电脑端和手机端的主流浏览器，包括 Chrome、Edge、Safari、Android Chrome 和 iOS Safari。
- 系统部署目标为阿里云服务器，首期实现业务功能，不要求数据库备份与恢复管理功能。
- 用户只能由管理员创建，不开放自主注册。
- 系统保留默认管理员账户 `admin / admin123`，首次登录后必须支持修改密码。
- 全国全景使用可交互的 3D 风格模拟地图，不要求接入真实地理地图数据。

## 13. 人体工程学和交互原则

系统设计必须优先考虑高频使用效率：

- 常用操作距离当前内容不超过一次点击
- 更新进展按钮固定在项目详情的明显位置
- 保存和取消按钮固定在表单底部
- 筛选条件不因页面滚动而完全消失
- 长表单按信息类别分组
- 低频字段默认折叠
- 重复信息只展示一次
- 不展示不能帮助决策的统计和装饰
- 按钮文字简短明确
- 危险操作必须二次确认
- 操作成功或失败必须有即时反馈
- 表格加载、保存和导入过程必须展示状态
- 错误信息应指出具体字段和处理方式
- 颜色不能作为唯一状态识别方式，应同时显示文字

前端易用性和视觉质量是首期验收重点。桌面端应适合长期台账办公，移动端应针对触摸操作重新组织布局，不得仅缩小桌面端页面。筛选、表单、详情、历史时间线、地图联动、加载状态、空状态、错误反馈和权限状态均必须完整设计并可用。

## 14. 视觉规范

项目台账页面采用浅色、清晰、适合长期办公的视觉风格；全国全景页面可以采用深色科技风格。

整体要求：

- 层级清晰
- 信息密度适中
- 表格边界明确
- 状态颜色克制
- 卡片和弹窗不过度嵌套
- 不使用与业务无关的装饰性图形
- 不使用影响阅读的持续动画
- 保证文字、按钮和标签在电脑端与手机端不重叠

## 15. 关键业务规则

1. 项目必须关联省份和项目名称。
2. 同一项目在不同省份必须分别维护。
3. 项目阶段状态和健康状态必须分开保存。
4. 健康状态为“有问题”时必须填写问题说明。
5. 每个项目必须设置更新周期。
6. 项目即使没有新进展，也必须在周期内提交更新。
7. 每次进展更新必须保留历史记录。
8. 历史记录原则上只追加，不直接覆盖。
9. 更新提醒只在项目台账中展示，不建设待办中心。
10. 全国全景的数据必须来源于项目台账。
11. 当前版本只维护省份，不增加地市层级。
12. Excel 导入不得破坏已有历史记录。
13. 当前版本不支持删除项目；项目数据只能新增、编辑或更新进展。

## 16. 实施计划

完整的技术方案、阶段任务、阶段产出、验收重点、交付顺序和依赖关系，统一维护在 [实施计划.md](实施计划.md)。实施过程应以该文档为执行依据，并以本规范中的业务规则作为最终验收标准。

## 17. 后续扩展方向

在不改变核心模型的前提下，后续可以扩展：

- 项目评论
- 项目收藏
- 常用筛选视图
- 数据变更通知
- 更细的字段级权限
- 项目健康状态趋势分析
- 按项目类型和年份的统计分析

这些功能都应围绕项目台账扩展，不重新引入待办中心或重复的数据维护入口。
