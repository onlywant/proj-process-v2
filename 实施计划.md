# 项目台账与全国全景系统实施计划

## 1. 文档定位

本文档是项目台账与全国全景系统的执行计划，依据《项目规范.md》编制。项目规范负责定义业务规则和产品边界，本文档负责拆解实施阶段、技术任务、交付物、验收重点和依赖关系。

## 2. 实施原则

- 以项目台账为唯一业务数据源，先建立稳定的数据模型和后端权限边界，再实现页面功能。
- 每个阶段都必须有可运行、可验证的交付结果，不将全部功能堆到最后一次验收。
- 前端体验与后端业务功能同等重要，桌面端和移动端均需在开发过程中持续验证。
- 所有涉及当前快照的更新操作必须同步生成历史记录和审计信息。
- 不实现规范中明确排除的项目删除、批量编辑、附件和通知功能。

## 3. 技术方案

- 后端：Go，提供登录、权限、项目台账、历史记录、统计、字典、用户、Excel 导入导出等 HTTP API。
- 数据库：SQLite，使用迁移脚本管理表结构和默认数据。
- 前端：Vue 3 + TypeScript + Vite，采用响应式布局和组件化实现桌面端、移动端页面。
- 地图：使用前端可视化方案实现 3D 风格模拟地图，省份区域、颜色、数量和交互数据均来自台账接口。
- 部署：前端构建为静态资源，由 Go 服务或 Web 服务器提供访问，部署到阿里云 Linux 服务器。
- 安全：密码使用安全哈希保存；登录会话、接口权限、管理员配置接口和用户停用状态均由后端校验。

## 4. 阶段划分

### 阶段一：项目初始化与基础工程

主要工作：

- 建立 Go 后端、Vue 前端和 SQLite 数据库工程。
- 配置开发、构建、环境变量和基础日志方案。
- 建立统一 API 响应格式、错误处理、前端路由和基础布局。
- 提供默认管理员初始化逻辑和基础健康检查接口。

阶段产出：

- 前后端可独立启动和联调。
- 登录页、基础应用框架和空白台账页可访问。
- 数据库可初始化，默认管理员可以登录。

### 阶段二：数据模型、认证和权限

主要工作：

- 设计用户、角色、项目、项目进展历史、字段审计、字典和导入批次等数据表。
- 实现登录、退出、修改密码、当前用户信息和管理员创建/停用用户。
- 实现管理员和普通用户的后端权限中间件。
- 实现默认项目阶段、健康状态、问题标签和更新周期字典。
- 实现管理员字典维护页面和接口。
- 将规范中的默认阶段、健康状态和问题标签完整写入初始化数据，并允许管理员维护。
- 将更新周期临近阈值作为管理员可配置的系统参数，默认值为 7 天；系统不得提供关闭周期检查的配置。

验收重点：

- 普通用户无法访问系统配置接口和页面。
- 停用用户无法登录或继续使用受保护接口。
- 默认密码首次登录后可以修改，密码不以明文保存。

### 阶段三：项目台账核心功能

主要工作：

- 实现项目新增、基础信息编辑和项目详情。
- 按“省份 + 项目名称 + 项目年份”建立唯一性校验。
- 支持项目编号作为同省份、同年份、同名项目的辅助唯一标识，并明确重复数据的处理提示。
- 按规范逐项实现全部项目字段，确保基础信息、金额和业务信息、当前状态信息均可新增、编辑、查询、导入和导出。
- 实现项目列表、分页、关键词搜索、组合筛选、排序和快捷筛选。
- 实现列配置、固定关键列、加载状态、空状态和错误反馈。
- 实现项目阶段、健康状态、问题说明和更新周期校验。

验收重点：

- 不同省份的同名项目能够独立维护。
- 健康状态为“有问题”时不能缺少问题说明。
- 桌面端能够高效浏览大表，移动端自动切换为卡片或分组列表。

### 阶段四：进展更新、历史和审计

主要工作：

- 实现项目进展更新表单。
- 保存更新时使用事务同步更新项目当前快照、生成历史记录、记录更新人并计算下次更新时间。
- 实现更新状态判断：正常、临近更新、逾期未更新。
- 在项目详情中以时间线展示历史记录。
- 对项目基础信息重要字段记录修改前后内容、操作人和操作时间。
- 实现历史修正流程：原始历史记录保持不变，通过新增修正记录记录修正内容、修正人、修正时间和关联的原始记录。

验收重点：

- 每次进展更新都能在历史时间线中追溯。
- 历史记录不能被普通编辑覆盖或删除。
- 历史错误可以通过新增修正记录处理，原始内容和修正关系均可追溯。
- 更新周期计算和提醒状态在列表、详情中保持一致。
- 每周、每两周、每月和自定义天数均可正确计算下次更新时间；临近阈值使用管理员配置值，且不能关闭检查。

### 阶段五：Excel 导入和导出

主要工作：

- 根据规范字段设计正式 Excel 导入模板和表头映射规则。
- 在模板和映射表中覆盖规范列出的全部项目字段，并保留未使用或扩展字段的兼容能力。
- 实现文件上传、表头识别、字段格式校验和必填校验。
- 按项目唯一性规则识别新增、更新、重复和错误记录。
- 同省份、同年份、同名项目存在项目编号时，使用项目编号辅助识别并在预览中说明匹配结果。
- 实现预览结果、用户确认、导入批次和操作人记录。
- Excel 中的进展更新形成新的历史记录，不覆盖原有历史。
- 实现当前筛选结果、完整台账和项目历史记录导出。

验收重点：

- 导入前必须预览和确认，错误记录必须显示具体原因。
- 重复导入不会产生无法识别的重复项目。
- 导入和导出字段名称、状态、更新周期和时间信息完整。
- 全部项目字段在导入预览、正式写入和导出结果中的映射一致。
- Excel 导入产生的历史记录明确标记来源为 Excel 导入，并可在项目历史中查看。

### 阶段六：全国全景

主要工作：

- 实现 3D 风格模拟中国省份地图。
- 根据接口统计结果显示省份项目数量和健康状态。
- 实现省份点击高亮、其他省份降亮、右侧汇总和项目列表。
- 实现地图按健康状态、阶段、类型、年份和更新状态筛选。
- 支持从地图项目进入项目详情或台账筛选结果。
- 控制动画频率和视觉层级，避免遮挡信息和影响操作。

验收重点：

- 地图数据完全来源于项目台账接口。
- 地图筛选结果与台账筛选结果口径一致。
- 桌面端和移动端均能查看省份汇总并进入项目详情。

### 阶段七：前端体验完善与兼容性验证

主要工作：

- 完善桌面端导航、台账工具栏、筛选区、详情抽屉或页面和个人菜单。
- 完善移动端“我的”、底部或适配导航、项目卡片、详情和固定操作区。
- 统一状态标签、按钮、表单校验、确认提示、成功反馈和错误反馈。
- 验证长表单、弹窗、时间线、地图和表格在不同屏幕尺寸下不重叠、不溢出。
- 验证 Chrome、Edge、Safari、Android Chrome 和 iOS Safari。
- 按信息类别组织项目表单，低频字段默认折叠；详情页展示完整字段且避免重复信息。
- 固定或保持可触达的更新、保存、取消和返回操作；筛选条件在列表滚动时保持可用。
- 按规范使用健康状态颜色：良好为绿色、正常为蓝色、有问题为红色、暂无信息为灰色；所有状态同时显示文字。

验收重点：

- 高频操作距离当前内容不超过一次点击。
- 移动端不依赖横向滚动完整宽表完成核心操作。
- 所有状态同时使用文字和颜色表达，不能只依赖颜色。
- 保存、取消、返回和更新操作在桌面端与移动端均可触达；移动端核心流程无需反复滚动寻找操作。
- 项目表单覆盖规范全部字段，字段分组、校验、错误提示和详情展示完整。

### 阶段八：测试、部署和上线验收

主要工作：

- 编写后端单元测试、API 测试和关键业务事务测试。
- 编写前端核心交互测试，覆盖登录、台账筛选、项目更新、历史查看、导入预览和权限控制。
- 补充历史修正、默认字典、项目编号辅助唯一性、更新周期配置、完整字段导入导出和地图统计一致性测试。
- 使用空数据库验证初始化、默认管理员、字典和首次登录改密流程。
- 构建生产前端和 Go 服务，完成阿里云 Linux 部署配置。
- 完成 HTTPS、服务自动启动、日志查看和基本运行监控配置。
- 根据验收清单进行桌面端、移动端和主要浏览器验证。

最终验收必须覆盖：

- 登录、用户管理和两类角色权限。
- 项目新增、编辑、搜索、筛选、详情和历史。
- 进展更新、更新周期和逾期状态。
- Excel 导入预览、确认、历史生成和导出。
- 全国全景地图联动。
- 移动端核心流程和主流浏览器兼容性。
- 历史修正、更新周期配置、完整字段映射和项目编号辅助唯一性。
- 普通用户访问系统配置接口、停用用户访问受保护接口和会话权限校验。
- 默认状态颜色、固定操作区、筛选保持和移动端长页面操作体验。

## 5. 交付顺序和依赖

阶段一是所有工作的基础；阶段二完成后才能进行可靠的业务权限验收。阶段三和阶段四构成台账核心，可并行推进部分前端页面和后端接口。阶段五依赖项目数据模型和历史机制，阶段六依赖统计接口，阶段七贯穿全部阶段并在阶段八集中验收。

每个阶段完成后应保留可运行版本和简短变更记录，发现问题优先修复影响数据一致性、权限和移动端核心操作的问题。
